# دليل ترقية مشروع FULLSCO إلى Next.js

## هيكل المشروع

### المجلدات الرئيسية

```
/
├── components/          # مكونات قابلة لإعادة الاستخدام
│   ├── layout/            # مكونات التخطيط (MainLayout, Header, Footer)
│   ├── ui/                # مكونات واجهة المستخدم الأساسية (Button, Sheet, etc.)
│   ├── forms/             # مكونات النماذج
│   ├── scholarships/      # مكونات خاصة بالمنح الدراسية
│   └── dashboard/         # مكونات لوحة التحكم
├── contexts/            # سياقات React
├── db/                  # إعدادات قاعدة البيانات وملفات البذر
├── hooks/               # React Hooks المخصصة
├── lib/                 # وظائف مساعدة وأدوات
│   ├── queryClient.ts     # إعداد React Query
│   ├── types.ts           # أنواع TypeScript
│   └── utils.ts           # وظائف مساعدة
├── pages/               # صفحات Next.js
│   ├── api/               # نقاط نهاية API
│   ├── _app.tsx           # مكون التطبيق الرئيسي
│   ├── _document.tsx      # مكون توثيق HTML
│   ├── index.tsx          # الصفحة الرئيسية
│   ├── 404.tsx            # صفحة 404
│   ├── scholarships/      # صفحات المنح الدراسية
│   ├── posts/             # صفحات المقالات
│   └── dashboard/         # صفحات لوحة التحكم
├── public/              # ملفات ثابتة
├── server/              # كود الخادم الخلفي
│   ├── controllers/       # وحدات تحكم API
│   ├── config/            # ملفات التكوين
│   ├── index.ts           # إعداد الخادم
│   └── routes.ts          # مسارات API
├── shared/              # ملفات مشتركة بين الواجهة والخلفية
│   └── schema.ts          # تعريفات مخطط قاعدة البيانات
└── styles/              # أنماط CSS
    └── globals.css        # أنماط CSS العالمية
```

## مواقع الصفحات الرئيسية

| الصفحة | المسار | الوصف |
|--------|--------|-------|
| الرئيسية | `/pages/index.tsx` | الصفحة الرئيسية للموقع |
| المنح الدراسية | `/pages/scholarships/index.tsx` | قائمة المنح الدراسية |
| تفاصيل المنحة | `/pages/scholarships/[slug].tsx` | صفحة تفاصيل منحة دراسية معينة |
| المقالات | `/pages/posts/index.tsx` | قائمة المقالات |
| تفاصيل المقالة | `/pages/posts/[slug].tsx` | صفحة تفاصيل مقالة معينة |
| البحث | `/pages/search.tsx` | صفحة البحث |
| لوحة التحكم | `/pages/dashboard/index.tsx` | لوحة التحكم الرئيسية |
| تحرير المنح | `/pages/dashboard/scholarships/[id].tsx` | تحرير منحة دراسية |
| تحرير المقالات | `/pages/dashboard/posts/[id].tsx` | تحرير مقالة |
| صفحة 404 | `/pages/404.tsx` | صفحة الخطأ 404 |

## توجيهات لتجنب التكرار

### 1. استخدام المكونات القابلة لإعادة الاستخدام

- **مكونات التخطيط** - استخدم `MainLayout` لمكونات الصفحة الرئيسية والعامة لتوفير شكل موحد.
- **مكونات البطاقات** - استخدم مكونات مثل `ScholarshipCard` و `ArticleCard` لعرض المنح والمقالات.
- **مكونات النماذج** - أنشئ مكونات نماذج قابلة لإعادة الاستخدام للحقول المتكررة.

### 2. مكتبة المستندات (CMS)

- تأكد من أن جميع مكونات مكتبة المستندات (الصور، والمستندات، إلخ) تستخدم نفس مكونات العرض.
- استخدم واجهة برمجة تطبيقات مشتركة للوصول إلى الملفات.

### 3. استراتيجيات معالجة الحالة

- استخدم React Query للبيانات المستخرجة من الخادم.
- استخدم Context API للبيانات العالمية مثل إعدادات الموقع والمصادقة.
- استخدم نفس سياق المستخدم في جميع أجزاء التطبيق.

### 4. استعلامات API

- استخدم هيكلًا موحدًا لاستعلامات API.
- تأكد من أن جميع نقاط النهاية تعيد نفس تنسيق الاستجابة.
- أنشئ خطاطات Zod للتحقق من البيانات.

### 5. التنميط

- استخدم متغيرات CSS المحددة في ملف `globals.css` للألوان والأحجام.
- استخدم أسلوب tailwind المتناسق عبر المشروع.
- لا تقم بإنشاء فئات CSS مخصصة إذا كان يمكن تحقيق نفس النتيجة باستخدام Tailwind.

### 6. تحميل البيانات

- استخدم GetServerSideProps أو GetStaticProps من Next.js لتحميل البيانات.
- حافظ على أسلوب متناسق عند تحميل البيانات.
- لا تستخدم أكثر من استراتيجية واحدة لجلب البيانات في التطبيق.

### 7. إدارة الحالات المتزامنة

- استخدم نفس آلية العرض لحالات التحميل (Skeleton UI مثلًا).
- استخدم نفس آلية العرض للأخطاء.
- استخدم نفس آلية العرض للحالات الفارغة.

### 8. الاتصال بقاعدة البيانات

- استخدم نموذج بيانات Drizzle الموجود في `shared/schema.ts`.
- لا تنشئ مخططات جديدة إلا في هذا الملف.
- استخدم المنسقات المحددة للاستعلامات المعقدة.

### 9. المصادقة

- استخدم نظام المصادقة المحدد في `server/controllers/auth-controller.ts`.
- تأكد من أن middleware المصادقة يتم تطبيقه بشكل متناسق.

### 10. الأخطاء وملاحظات التصحيح

- حافظ على أسلوب متناسق في تسجيل الأخطاء.
- تأكد من أن جميع الاستثناءات يتم التقاطها ومعالجتها.

## إرشادات عامة لإكمال عملية الترقية

1. **ابدأ بالنواة** - ركّز أولاً على الصفحات الرئيسية.
2. **تنفيذ تدريجي** - أضف ميزات واحدة تلو الأخرى.
3. **ابدأ بالواجهة الأمامية** - اجعل الواجهة الأمامية تعمل أولاً، ثم أضف الوظائف الخلفية.
4. **اختبر كثيرًا** - اختبر كل ميزة بعد إضافتها.
5. **حافظ على التوثيق** - حدّث التقرير مع تقدم الترقية.

## مسار الترقية المقترح

1. إكمال الصفحة الرئيسية والتنقل
2. تنفيذ صفحات المنح الدراسية
3. تنفيذ البحث والتصفية
4. إضافة صفحات المقالات
5. تكامل لوحة التحكم
6. تنفيذ المصادقة
7. إضافة محرر محتوى
8. إنشاء صفحات رئيسية متقدمة ومخصصة