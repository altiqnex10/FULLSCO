# تقرير استراتيجية ترحيل المشروع

## نظرة عامة على الاستراتيجية

استراتيجية الترحيل تتبع نهجاً تدريجياً ومنظماً للانتقال من النظام الحالي (React.js مع Express.js) إلى النظام الجديد (Next.js مع الاحتفاظ بـ Express.js للخدمات المتخصصة). المقاربة تضمن استمرارية عمل النظام أثناء عملية الترحيل وتقليل المخاطر.

### المراحل الرئيسية للاستراتيجية

1. **تحليل وتخطيط (10%)**
   - تحليل البنية الحالية وتحديد المكونات والخدمات
   - رسم خريطة التبعيات بين المكونات
   - تحديد أولويات الترحيل
   - إنشاء خطة تنفيذية مفصلة

2. **إعداد البنية التحتية (15%)**
   - إعداد مشروع Next.js 
   - تكوين الاتصال بقاعدة البيانات PostgreSQL
   - دمج Express.js مع Next.js لفترة انتقالية
   - إعداد البنية التحتية لـ API Routes

3. **ترحيل نماذج البيانات (15%)**
   - ترحيل مخططات قاعدة البيانات (schemas)
   - ضمان توافق Drizzle ORM
   - التأكد من استمرارية عمل العلاقات بين الجداول

4. **ترحيل واجهة API (20%)**
   - إنشاء نقاط API جديدة بنمط Next.js
   - مطابقة السلوك الحالي للـ API
   - تأكيد التوافق مع طلبات الخدمة الحالية

5. **ترحيل مكونات واجهة المستخدم (25%)**
   - ترحيل المكونات الأساسية
   - تحديث المكونات لتتوافق مع Next.js
   - توفير دعم RTL للعربية
   - ترحيل الصفحات والمسارات

6. **تحسين الأداء وSEO (10%)**
   - تنفيذ Server-Side Rendering (SSR) حيث يكون مفيداً
   - تحسين أداء التحميل
   - تنفيذ استراتيجيات SEO المحسنة

7. **الاختبار والتصحيح (5%)**
   - اختبار كل المكونات والوظائف
   - معالجة أي مشاكل متعلقة بالتوافق
   - ضمان تجربة مستخدم سلسة

## ما تم إنجازه حتى الآن وما تبقى

### ما تم إنجازه (30%)

1. **تحليل وتخطيط (10/10)**
   - ✓ تحليل البنية الحالية وفهم النظام
   - ✓ تحديد الجداول والعلاقات في قاعدة البيانات
   - ✓ تحديد المكونات الأساسية للترحيل
   - ✓ وضع خطة ترحيل

2. **إعداد البنية التحتية (10/15)**
   - ✓ إعداد مشروع Next.js الأساسي
   - ✓ تكوين الاتصال بقاعدة البيانات 
   - ✓ دمج Express.js مع Next.js باستخدام server-next.js
   - ⚠️ إعداد وتكوين Drizzle ORM بالكامل (تم حل مشكلة الاتصال مع postgres-js)
   - ⚠️ تكامل بيئة التطوير (تكوين الـ dev server)

3. **ترحيل نماذج البيانات (5/15)**
   - ✓ ترحيل المخططات الأساسية (shared/schema.ts)
   - ⚠️ ترحيل مخططات العلاقات المتبقية
   - ❌ تجهيز آليات جلب البيانات المرتبطة (relations)

4. **ترحيل واجهة API (5/20)**
   - ✓ إنشاء نقاط API أساسية (site-settings, categories, levels, countries)
   - ⚠️ تحسين رسائل الخطأ ومعالجة الاستثناءات
   - ❌ إنشاء مسارات API لبقية الموارد
   - ❌ نقل التصديق والصلاحيات (Authentication & Authorization)

### ما تبقى (70%)

5. **استكمال ترحيل واجهة API (15%)**
   - ❌ إنشاء API للمنح الدراسية بالميزات المطلوبة
   - ❌ إنشاء API للمستخدمين والمصادقة
   - ❌ إنشاء API للمقالات وقصص النجاح
   - ❌ إنشاء API للشركاء والإحصائيات

6. **ترحيل مكونات واجهة المستخدم (25%)**
   - ⚠️ ترحيل مكون MainLayout (بدأنا بالعمل عليه)
   - ❌ ترحيل مكونات التنقل (Navigation)
   - ❌ ترحيل مكونات الرئيسية (Home)
   - ❌ ترحيل مكونات صفحات المنح الدراسية
   - ❌ ترحيل مكونات صفحات المقالات
   - ❌ ترحيل مكونات الإدارة (Dashboard)
   - ❌ ترحيل محرر المحتوى الغني

7. **تحسين الأداء وSEO (10%)**
   - ⚠️ تنفيذ دعم RTL (الترميز موجود، بحاجة لتطبيقه)
   - ❌ تحسين استراتيجيات SSR/CSR 
   - ❌ تنفيذ وسوم ميتا SEO لجميع الصفحات
   - ❌ تحسين أوقات التحميل والأداء

8. **الاختبار والتصحيح (5%)**
   - ❌ اختبار كل الوظائف
   - ❌ معالجة مشاكل التوافق وحالات الخطأ
   - ❌ تحسين تجربة المستخدم

## التحديات الحالية والخطوات القادمة

### التحديات المباشرة التي تم تحديدها:

1. **قاعدة البيانات:**
   - خطأ يشير إلى أن جدول site_settings غير موجود
   - قد تكون هناك حاجة للتحقق من إنشاء الجداول أو تحديث المخططات

2. **مكتبات وتبعيات:**
   - تم حل مشكلة استيراد drizzle-orm/pg-pool بالانتقال إلى postgres-js
   - قد تكون هناك حاجة لتعديلات إضافية على تكوين المكتبات

### الخطوات القادمة المباشرة:

1. **إكمال إعداد قاعدة البيانات:**
   - التحقق من وجود الجداول المطلوبة
   - تنفيذ سكريبت تهيئة لإنشاء الجداول إذا لم تكن موجودة

2. **استكمال نقاط الـ API الأساسية:**
   - استكمال وتحسين واجهات API
   - إضافة المزيد من معالجة الأخطاء والتحقق

3. **البدء بترحيل واجهة المستخدم:**
   - ترحيل الصفحات الأساسية مثل صفحة المنح الدراسية
   - تنفيذ دعم RTL للمحتوى العربي

4. **تحسين التكامل بين Next.js و Express.js:**
   - تأكد من أن توجيه الطلبات يعمل بشكل صحيح
   - تحسين آلية المتابعة والتسجيل

## خطة العمل المقترحة للخطوات التالية:

1. تصحيح مشكلة عدم وجود جدول site_settings وإنشاء جداول قاعدة البيانات
2. إكمال نقاط API للموارد الأساسية (المنح، المقالات، المستخدمين)
3. إنشاء صفحات أساسية: الصفحة الرئيسية، وصفحة قائمة المنح، وصفحة تفاصيل المنحة
4. تطبيق دعم RTL والتصميم المناسب للمحتوى العربي
5. ترحيل نظام المصادقة والصلاحيات

هذه الخطة ستعالج أهم التحديات الحالية وتضع أساساً قوياً لاستكمال عملية الترحيل.

## تحديثات التقدم

### تاريخ: 06 مايو 2025
- أنشأنا ملف التقرير
- أكملنا تحليل البنية الحالية
- إصلاح مشكلة الاتصال بـ postgres-js
- أنشأنا نقاط API أولية (site-settings, categories, levels, countries)